application.title=grid
application.vendor=stgreen
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/grid.jar
dist.javadoc.dir=${dist.dir}/javadoc
jnlp.codebase.type=local
jnlp.codebase.url=file:/Users/plamere/work/aura/SitmWebServices/grid/dist
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
project.sitm=../sitm
reference.sitm.jar=${project.sitm}/dist/sitm.jar
# A place where tool libraries live.
tool.libraries=../ToolLibraries
excludes=
includes=**
jar.compress=false
javac.classpath=\
    ${reference.aura.jar}:\
    ${reference.aardvark.jar}:\
    ${reference.sitm.jar}:\
    ${libs.Caroline.classpath}:\
    ${libs.Minion.classpath}:\
    ${libs.SunLabsUtilities.classpath}:\
    ${libs.JiniCore.classpath}:\
    ${libs.BerkeleyDB.classpath}:\
    ${libs.SITMUtil.classpath}:\
    ${libs.Rome.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=com.sun.labs.aura.grid.ServiceDeployer
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=JDK_1.6
project.aardvark=../aardvark
project.aura=../aura
reference.aardvark.jar=${project.aardvark}/dist/aardvark.jar
reference.aura.jar=${project.aura}/dist/aura.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
